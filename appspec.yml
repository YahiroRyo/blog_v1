version: 0.0
os: linux
files:
  - source: /
    destination: /var/tmp/
hooks:
  AfterInstall:
    - location: /scripts/after_install.sh
      timeout: 300
      runas: root
  ApplicationStart:
    - location: /scripts/application_start.sh
      runas: root